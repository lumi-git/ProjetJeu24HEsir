package Utils;

public class Config {
    //use these variables in the game panel to triger som parameters
    //(temporary, replace a real config file)
    public boolean showHitbox = true;
    public boolean showFPS = false;
    public Config() {

    }

}
